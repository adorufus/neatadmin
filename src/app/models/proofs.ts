import { Timestamp } from "firebase/firestore";

export class Proofs {
    area_name: string = "";
    created?: Timestamp;
    floor_number: number = 0;
    image_url: string = "";
    pic: string = "";
    task_name: string = "";
}
